var BaseApiUrl;

function buildUrl(service) {
    return BaseApiUrl + service;
}

window.onload = function() {
    var vm = new Vue({
        el: '#app',
        data: {},
        mounted() {},
        methods: {

        }
    });
}